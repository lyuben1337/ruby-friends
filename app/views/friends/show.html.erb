<%= render @friend %>

<div class="mt-4">
  <%= link_to "Edit", edit_friend_path(@friend), class:"btn btn-secondary"%>
  <%= link_to "Back", friends_path, class:"btn btn-secondary" %>
  <td><%= link_to "Delete", @friend, data: {
    "turbo-method": :delete,
    "turbo-confirm": "Are you sure?"
  }, class:"btn btn-secondary" %></td>
</div>
